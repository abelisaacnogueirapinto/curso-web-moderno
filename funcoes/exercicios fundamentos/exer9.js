function repetir(item, quantidade) {
    return Array(quantidade).fill(item)
    }

   console.log( repetir ( 'abel', 5))

   function simboloMais(quantidade) {
    return "+".repeat(quantidade) /// repete o simbolo + tantas vezes recebido em quantidade param
   }

   
   console.log(simboloMais(20))

    

   function simboloMais2(simbolo,quantidade) {
       console.log( simbolo.repeat(quantidade))
   }
simboloMais2('_', 120)


        